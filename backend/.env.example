# Backend Environment Configuration
# Copy this file to .env and fill in the values

# Database Configuration
# Neon Serverless PostgreSQL connection string
# Format: postgresql+asyncpg://user:password@host/database
# Note: SSL is configured in code for asyncpg compatibility
DATABASE_URL=postgresql://neondb_owner:npg_pQt2Z4sgyhbf@ep-rapid-violet-ahvvy4sw-pooler.c-3.us-east-1.aws.neon.tech/neondb&channel_binding=require

# Application Configuration
# Set to "development" or "production"
ENVIRONMENT=development

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Authentication Configuration
# URL to fetch JWKS from Better Auth frontend
JWKS_URL=http://localhost:3000/api/auth/jwks
